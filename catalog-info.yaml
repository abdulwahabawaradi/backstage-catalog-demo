apiVersion: backstage.io/v1alpha1
kind: Component

metadata:
  name: demo-service

spec:
  type: service
  owner: platform-team
  lifecycle: production
  system: ecommerce-platform
  providesApis:
    - demo-api
